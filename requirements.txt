# Core ML and Computation
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.21.0
scipy>=1.7.0

# 3D Processing and Utilities
trimesh>=3.10.0
chumpy>=0.70  # Often required by FLAME implementations

# Note: PyTorch3D usually needs to be installed via conda or specific wheels
# depending on your CUDA version, rather than a simple pip install.
# e.g., conda install pytorch3d -c pytorch3d

# Image Processing and Visualization
opencv-python>=4.5.0
matplotlib>=3.5.0
Pillow>=9.0.0

# Graph Neural Networks (For the downstream extension phase if choosing GCNs)
# torch_geometric 

# Jupyter for EDA
notebook>=6.4.0